spring.application.name=apprenant-service

spring.datasource.url=jdbc:mysql://localhost:3306/apprenantservicedb?createDatabaseIfNotExist=true

spring.datasource.username=root
spring.datasource.password=123456789
#spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update

server.port=8081

# Feign Configuration
feign.client.config.default.loggerLevel=full
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=5000

# Brief Service URL
brief.service.url=http://localhost:8081

spring.jpa.show-sql=true
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
spring.jpa.properties.hibernate.format_sql=true
#spring.jackson.serialization.write-dates-as-timestamps=false
#spring.jackson.date-format=yyyy-MM-dd

logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

eureka.client.serviceUrl.defaultZone=http://localhost:8080/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Force registration with IP instead of hostname
eureka.instance.prefer-ip-address=true
eureka.instance.ip-address=127.0.0.1

# Cleaner instance ID format
eureka.instance.instance-id=${spring.application.name}:${server.port}
